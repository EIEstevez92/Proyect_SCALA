testsparkjob {

  # Config for testsparkjob

}

sparkMetrics {

  listeners = ["default"]

  output {
    type = "kafka"
    topic = "topic-metrics-testsparkjob"
    key = "testsparkjob-test"
    bootstrapServers = "kafka:9092"
  }
}
